==== Assignment1 ====

Both the ENC and DEC modules have separate Makefiles to run them. The outputs are stored in a text file.  

1. AES_ENC_CPP  
   make // compiles the encryption module  
   make clean // removes AES_ENC_128_out.txt

2. AES_DEC_CPP  
   make // compiles the decryption module  
   make clean // removes AES_DEC_128_out.txt

Use with Caution:  
This code is provided as-is and should be used with caution. It has been tested in specific contexts but might behave differently on your system depending on configurations and dependencies. Ensure you validate the outputs 
and performance before relying on it for critical operations.  
